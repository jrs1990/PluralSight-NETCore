@if (User.Identity.IsAuthenticated)
{
    <div>Hello, @User.Identity.Name</div>
    <a class="btn btn-default" asp-controller="Account" asp-action="Logout"/>
}
else
{
    <a class="btn btn-default" asp-controller="Account" asp-action="Login">Login</a>
}
